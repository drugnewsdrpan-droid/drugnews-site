# Inbox

Put one folder per article here. The publisher reads due articles and moves them to `content/published/`.

Required files:

- `article.md`
- `meta.json`
- optional `images/`

Run:

```bash
npm run publish
```
